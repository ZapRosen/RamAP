# Raspi als Hotspot Die IP-Adresse sollte außerhalb des Routerbereichs liegen
# Wenn die IP-Adresse geändert werden soll, muss sie kongruent in der Datei dnsmasq.conf geändert werden.

allow-hotplug wlan0
iface wlan0 inet static
	address 192.168.255.1
	netmask 255.255.255.0
	wireless-mode Master
	wireless-power off

